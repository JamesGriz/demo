import type { Meta, StoryObj } from '@storybook/react'
import { {{properCase name}} } from './'

const meta = {
  title: '{{type}}/{{properCase name}}',
  component: {{properCase name}},
  tags: ['autodocs'],
} satisfies Meta<typeof {{properCase name}}>

export default meta
type Story = StoryObj<typeof meta>

// Add your specific story configurations here
export const Default: Story = {
  args: {
    // populate with required props, use control + space for auto suggestions
  },
}

// Add more stories as needed